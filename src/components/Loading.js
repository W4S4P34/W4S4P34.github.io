const Loading = () => {
  return (
    <div
      id="loading"
      className="flex flex-col flex-auto justify-center items-center"
    >
      Loading...
    </div>
  );
};

export default Loading;

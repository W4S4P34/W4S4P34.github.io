const Loading = () => {
  return (
    <div
      id="loading"
      className="flex flex-col flex-auto p-4 justify-center items-center"
    >
      Loading...
    </div>
  );
};

export default Loading;
